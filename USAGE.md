# Doloris (ドロリス) 完整使用手册与操作指南

> 面向 AI Coding Agent (Codex / Claude) 的桌宠级长任务无人值守托管与自愈系统

---

## 目录
1. [快速上手（1 分钟极速指南）](#1-快速上手1-分钟极速指南)
2. [双重使用形态：桌宠伴侣与终端 CLI](#2-双重使用形态桌宠伴侣与终端-cli)
3. [三大托管模式深度剖析与选型矩阵](#3-三大托管模式深度剖析与选型矩阵)
4. [桌宠桌面伴侣 (Desktop Mascot) 完整交互指南](#4-桌宠桌面伴侣-desktop-mascot-完整交互指南)
5. [L1 看门狗与 L2 委托自愈机制](#5-l1-看门狗与-l2-委托自愈机制)
6. [任务编写与交付验收标准 (Task & Acceptance)](#6-任务编写与交付验收标准-task--acceptance)
7. [运行产物、终态与日志排查](#7-运行产物终态与日志排查)
8. [常用命令行参数与环境变量全览](#8-常用命令行参数与环境变量全览)

---

## 1. 快速上手（1 分钟极速指南）

当你让 Codex 或 Claude 执行一项长流程任务，需要离开座位（开会、吃饭、睡觉）时，只需通过以下两种方式之一交接给 Doloris：

### 方式 A：桌宠图形界面（零记忆门槛，推荐）
1. 双击运行根目录下的 `doloris-app.vbs`（或在控制台运行 `.\doloris-app.cmd`）。
2. 桌面右下角出现可爱的 Doloris 桌宠。
3. **右键点击桌宠** -> 展开「🚀 开始托管」->「Fork 无头续跑」-> 点击最近活跃的会话。
4. 桌宠头顶弹出工作气泡，戴上安全帽进入代班打工状态。你现在可以安心离席或锁屏！

### 方式 B：终端命令行一键托管
打开 PowerShell 进入当前工程目录，直接执行：
```powershell
# 推荐：一键 Fork 托管最新活跃会话（不杀 App，父任务安全暂停，子任务无头续跑）
.\doloris.cmd
```

---

## 2. 双重使用形态：桌宠伴侣与终端 CLI

Doloris 提供了**“桌面伴侣桌宠”**与**“终端命令行 CLI”**两套无缝协同的交互形态，底层共享完全一致的监管引擎：

```
                    ┌────────────────────────┐
                    │      开发者 (离席/AFK)   │
                    └───────────┬────────────┘
                                │
             ┌──────────────────┴──────────────────┐
             ▼                                     ▼
   【桌宠伴侣 GUI 形态】                   【终端 CLI 形态】
   .\doloris-app.cmd / .vbs              .\doloris.cmd [fork|resume|gui]
   - 桌面透明悬浮窗 (5%~500%缩放)         - 纯无头 / 脚本化 / CI 环境
   - 动态漫画气泡实时感知状态              - 丰富参数微调与批量回归
   - 右键直觉选择会话与模式               - 完整终端彩色日志与进度监控
             │                                     │
             └──────────────────┬──────────────────┘
                                ▼
               ┌─────────────────────────────────┐
               │    Doloris 核心监管引擎 (L1)    │
               │   afk_supervisor + controller   │
               └────────────────┬────────────────┘
                                │ 遇到选择题/可修复报错
                                ▼
               ┌─────────────────────────────────┐
               │   L2 委托决策与自愈 (Antigravity) │
               │   DECIDE / REPAIR / REVIEW 协议 │
               └─────────────────────────────────┘
```

---

## 3. 三大托管模式深度剖析与选型矩阵

Doloris 提供了三种完全不同的任务交接与运行模式，满足从低资源静默运行到高可视现场审计的不同需求：

| 特性维度 | 模式 1：Fork 无头续跑 (推荐) | 模式 2：经典安全接管 (Kill & Resume) | 模式 3：双有头原生 GUI 注入 |
|---|---|---|---|
| **一键命令** | `doloris.cmd fork` 或 `afk2.cmd` | `doloris.cmd resume` 或 `afk.cmd` | `doloris.cmd gui` 或 `afk3.cmd` |
| **桌面端 App 状态** | **保持打开**（前台窗口静止） | **自动退出**（释放操作系统写锁） | **保持打开**（前台窗口实时生成） |
| **任务会话关系** | 原会话安全暂停，派生新子 Thread | 在原会话 Thread 原地 headless resume | 在原会话内通过 UI 界面直接对话 |
| **Token 并发安全** | 严格杜绝（无限等待父任务暂停确认） | 绝对安全（App完全关闭后接管） | 绝对安全（单窗口单任务串行交互） |
| **系统资源消耗** | 极低（子任务无头执行） | 极低（无图形界面，最省内存） | 中等（需维持 Electron 渲染与动画） |
| **锁屏/熄屏支持** | 完全支持 | 完全支持 | 需保持桌面会话（Windows Session）活跃 |
| **适用场景** | **日常工作首选**：保留桌面现场与历史 | 睡觉过夜/远程服务器/极简低资源静默 | 想要直观在屏幕上看模型实时打字与生成 |

### 模式 1 详解：Fork 无头续跑（`doloris.cmd fork`）
* **核心机制**：
  1. 启动时首先获取工作区监管互斥锁，并在磁盘持久化 `HANDOFF_WAIT` 状态。
  2. 向 Codex App 发送暂停请求，进入**无限等待暂停确认**：每秒扫描 rollout 轨迹，直到结构化事件证实 `turn_aborted` 或 `task_complete`，且**所有在途工具调用（call/output）均已闭环**。
  3. 确认原任务彻底停稳后，无头子任务拉起（`codex exec fork`）。
  4. 若父任务停在提问上，先交由 L2 拍板；若为常规交接停顿，自动安全注入「继续」，开启无头推进。
* **安全性**：彻底解决了老式脚本强行 fork 导致的父子双重并发、重复消耗 Token 和写锁冲突。

### 模式 2 详解：经典安全接管（`doloris.cmd resume`）
* **核心机制**：
  1. **安全边界精确核验**：按 `call_id` 逐一配对工具调用。若当前正在跑复杂命令或写大文件，看门狗耐心地等待其安全结束（默认最多等待 90 秒，可自定义）。
  2. 确认安全后优雅退出 Codex 桌面进程。
  3. 通过操作系统文件锁探测验证 `~/.codex/thread-writer-locks/` 已真正释放（绝不采用粗暴删除锁文件的危险手段）。
  4. 启动 headless 原地 resume 原会话，续跑历史完全保留在原 Thread。
* **安全性**：杜绝“表面文件 15 秒没动静就盲杀导致代码写一半损坏”的灾难性隐患。

### 模式 3 详解：双有头原生 GUI 注入（`doloris.cmd gui`）
* **核心机制**：
  1. Codex 桌面窗口全程显示在前台。
  2. 基于 Windows 原生 UI Automation (UIA)，通过 `active-thread-<SID>` 与 `codex://threads/<SID>` 机器元数据进行**双重机器身份校验**。严格拒绝非目标面板、拒绝模糊标题匹配、拒绝正文文本误匹配。
  3. 定位目标输入框，执行原子写入并触发提交，监听新指令接收证据（`GUI_INJECT_ACK`），严密防止网络抖动导致的重复发送。
* **只读预检（注入前自证）**：发送任何按键前先跑一次 `gui_inject.ps1 -ProbeOnly` 只读身份探针（不敲键、不点按、不写剪贴板）；若判定为环境性不可送达（例如桌面端以更高权限运行、UIA 拒绝访问），立刻给出中文结论并停止发送，不再空耗有界重试预算。现场排查：`python tests/check_gui_probe.py --sid <会话 SID>`。

---

## 4. 桌宠桌面伴侣 (Desktop Mascot) 完整交互指南

Doloris 桌宠拥有高精度骨骼动画与状态感知能力：

### 基础交互手势
* **拖拽移动**：鼠标左键按住桌宠身体，可随意拖拽停靠在屏幕任何位置。
* **平滑无级缩放**：
  * **快捷滚轮**：按住键盘 `Ctrl` 键并滚动**鼠标滚轮**，可在 **5% 到 500%** 之间实时无级缩放。
  * **精确输入**：右键菜单选择「🔍 调整显示大小」，直接输入百分比数值（如 `80` 代表 80%，`150` 代表 150%）。
* **动态漫画气泡**：
  * 桌宠在不同监管阶段会主动冒出气泡反馈进度。
  * 任务终态完成时，气泡会显示“任务大功告成！点击我查看报告”，**直接鼠标左键点击气泡**即可瞬间打开 Markdown 终态报告！

### 右键功能菜单
* **🚀 开始托管**：
  * 动态读取系统最近 8 个活跃会话，显示每个会话的标题摘要与更新时间，鼠标点击一键开跑。
  * 提供「完整会话选择器」弹窗，支持检索更多历史会话或手动粘贴 `codex://threads/...` URL。
* **⏹️ 停止托管**：随时一键优雅终止当前后台监管进程，自动清理子进程树，桌宠恢复闲逛。
* **🎨 更换桌宠皮肤**：实时无缝切换外观。
* **📄 查看最新终态报告**：直接用系统默认编辑器调出最新的 `report.md`。

### BYOP (Bring Your Own Pet) 自备皮肤导入规范
Doloris 支持用户自由导入自定义桌宠素材包。素材可存放在当前目录的 `./pets/` 或用户主目录的 `~/.codex/pets/`：
```text
pets/
└── my_pet/
    ├── pet.json             # （可选）元数据配置文件
    ├── spritesheet.png      # 方式 1：标准 1536x2288 Codex V2 动作大图（自动切片）
    └── states/              # 方式 2：分状态散图目录
        ├── idle/            # 01.png, 02.png...
        ├── running/         # 打工中动画
        ├── waiting/         # 思考中动画
        ├── failed/          # 报错抢修动画
        └── review/          # 审查通过动画
```

---

## 5. L1 看门狗与 L2 委托自愈机制

### 两级协同工作流
1. **L1 看门狗（afk_supervisor）**：
   * 负责守门：进程守护、心跳保活、网络 429 退避重试、崩溃重启。
   * 梯度挂死检测：面对大上下文生成，采用 `600s -> 1200s -> 1800s` 动态放宽阈值，避免误杀思考中的模型。
2. **L2 委托智能（Antigravity AGY 桥接）**：
   * 当任务遇到需要人工确认的二选一、视觉方向选择或遇到本地运行报错时触发。
   * **DECIDE 协议**：根据工程现有技术栈和架构规范，替不在场的人类做出最优技术决断，清洗格式后回喂 Worker，绝不挂起等待人工！
   * **REPAIR 协议**：抓取报错上下文，制定替代修复方案，命令 Worker 纠偏。
   * **REVIEW 协议**：任务宣称完成时，核对真实文件是否存在、测试是否通过，杜绝口头完工幻觉。

---

## 6. 任务编写与交付验收标准 (Task & Acceptance)

如果你需要通过 Doloris 调度一个全新的任务，推荐在 `tasks/<任务名>/` 目录下编写两个标准契约文件：

### 1. `tasks/<任务名>/task.md`（任务描述文件）
全文将作为任务初始 Prompt 下发给 Worker。必须明确：
* 交付物目录约定（默认候选是任务工作根下的 `work/`，也可在任务文本中写明绝对路径，或用 `--delivery-dir` 显式覆盖）。
* 授权范围与规范（告知 Worker 常规技术决策已授权主管，可直接提问）。

交付目录不存在或不可写时会进入基线 `blockers` 并阻止静默回退到会话根目录扫描；`--work-dir` 只影响该候选目录的推导，不会改变 `acceptance.md` 的解析基准。

### 2. `tasks/<任务名>/acceptance.md`（机器可验证的验收清单）
每行定义一条断言，只有全部满足才允许判为 `SUCCESS`：
```text
# 1. 验证进度清单勾选数（work/PROGRESS.md 内 '- [x]' 数量必须 ≥ 12）
checklist: work/PROGRESS.md :12

# 2. 验证交付物非空存在
work/report.md

# 3. 验证生成的文件数量（work/chapters 目录下必须存在至少 12 个 ch*.md 文件）
work/chapters/ch*.md :12
```

解析规则与安全边界：
* 所有断言路径一律相对**目标会话工作根**解析（`--adopt` 时是被接管会话的 cwd，否则是 Doloris 所在根目录），而不是相对 `--work-dir` 或 `--delivery-dir`。若产物在 `work/` 下，必须写作 `work/...`。
* `<相对 glob> :N` 要求至少 N 个位于工作区内的非空文件，省略 `:N` 时 N=1；`checklist: <路径> :N` 要求文件内 `- [x]` / `- [X]` 数量至少为 N，`checklist:` 后与 `:N` 前允许空格。
* `#` 开头的行为注释；空白规范、纯注释或任何无法满足的断言都按失败处理（fail-closed）。绝对路径、UNC 路径、`..` 以及 glob 等价的目录穿越写法会被直接拒绝。
* 存在 `acceptance.md` 时优先于旧版 12 章与默认契约；只有显式传入 `--selftest-12ch` 才会启用 `work/PROGRESS.md` 12 项勾选 + 12 个章节文件的兼容验收。无 `acceptance.md` 且未启用该参数时，使用默认契约：交付目录内 `PROGRESS.md` 全部勾选且 `report.md` 非空。

---

## 7. 运行产物、终态与日志排查

每次托管的所有事件、日志与快照均整洁存放在 `runs/<时间戳>/` 目录下：

```text
runs/20260921-225037-9ffd/
├── report.md                                # 终态报告：最优先阅读，包含完整时间线与验收明细
├── interventions.jsonl                      # 全生命周期结构化事件流（每次暂停、注入、切换均有据可查）
├── backup-pre-adopt-*.zip                   # 接管前自动打的工作区轻量快照，确保原工程绝对安全
├── agy_session.json                         # L2 会话上下文绑定信息
├── prompt.txt / resume-prompt.txt           # 初始与续跑提示词记录
├── supervisor_state.json                    # 最终内部状态机落盘
└── worker-stdout.log / worker-stderr.log    # 原始标准输出/错误流（排错核心）
```

### 终态语义说明
* **SUCCESS**：真实交付物通过断言校验，且（若开启 L2）通过终态审查。
* **FAILED**：交接条件不满足、安全检查未通过或有界恢复预算耗尽。
* **BLOCKED**：L2 确认遇到不可逾越的外部阻碍（如上游服务端封禁），给出停止依据后主动熔断。
* **CANCELLED**：用户主动按 `Ctrl+C` 或通过桌宠菜单点击停止托管。

### Webhook 外部推送通知
只需配置系统环境变量 `AFK_WEBHOOK_URL`，在任务达成终态时，Doloris 会自动将状态卡片与报告摘要推送到您的移动端（支持飞书、钉钉、企业微信机器人或 Bark）。

---

## 8. 常用命令行参数与环境变量全览

对于终端 CLI 用户，可通过参数进行细粒度控制：

```powershell
# 语法结构
doloris [--adopt TARGET] [--fork | --resume | --gui] [其他选项...]
```

| 参数项 | 默认值 | 作用说明 |
|---|---|---|
| `--adopt` | `last` | 指定接管目标：`last`（最新会话）、`1~8`（序号）、`codex://threads/...` 或会话 UUID |
| `--fork` | 默认开启 | 启用 Mode 1：保留 App，原任务无限等待暂停，子任务无头续跑 |
| `--resume` | 否 | 启用 Mode 2：安全退出 App，操作系统核查写锁释放，原任务 headless 续跑 |
| `--gui` | 否 | 启用 Mode 3：原生双有头 GUI 注入，App 全程前台活跃 |
| `--quick` | 关 | 快速交接模式：跳过交互式提问，直接选用当前上下文 |
| `--yes` | 关 | 免确认模式：自动接受所有交接确认提示 |
| `--work-dir` | `work` | 推导默认交付目录时的候选相对路径；不改变 `acceptance.md` 相对工作区根的解析基准 |
| `--delivery-dir` | 自动检测 | 显式指定用户交付目录；路径不存在或不可写会记为阻塞 |
| `--writable-root` | 会话工作目录 | 额外授权可写根目录，可重复指定 |
| `--verification-plan` | 空 | 启动时固化的功能验证计划 JSON；Worker 无法中途替换授权命令 |
| `--selftest-12ch` | 关 | 仅在无 `acceptance.md` 时启用旧版 12 章写作验收 |
| `--inspect-run` | 空 | 只读加载 `runs/<时间戳>/supervisor_state.json` 并输出 JSON 诊断摘要，不启动监管 |
| `--max-run-sec` | `0` (不设限) | 任务总时长预算（秒），默认跑完为止，超时主动熔断 |
| `--handoff-timeout-sec` | `90` | 模式 2 (kill) 检查在途工具调用安全退出的等待时限 |
| `--l2-cmd` | `antigravity` | L2 智能委托通道：`antigravity`（默认）、`off`（禁用） |
| `--l2-model` | `flash` | L2 模型选择：`flash_lite`、`flash`、`pro` |

*项目开发与贡献指南请参阅 [CONTRIBUTING.md](CONTRIBUTING.md)；系统安全红线与状态机规范请参阅 [docs/SPECIFICATION.md](docs/SPECIFICATION.md)。*
