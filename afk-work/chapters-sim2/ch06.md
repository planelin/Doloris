# 依赖超时

依赖超时发生在Agent等待数据库、外部API、消息队列或子任务时超过预期期限。它既可能是下游真正缓慢，也可能是连接池耗尽、DNS异常、锁竞争或被取消的请求没有及时释放资源。若所有层级都使用相同超时时间，故障会被无限放大，因此调用链应设置逐层递减的预算，并为连接、读取和整体任务分别计时。超时后先根据请求是否幂等决定是否重试，重试必须配合指数退避与随机抖动；对不保证幂等的写操作，只能查询状态或交由补偿流程处理。同时记录依赖名称、耗时、重试次数和追踪标识，建立熔断与降级策略，避免单个慢依赖拖垮整个编排过程。
