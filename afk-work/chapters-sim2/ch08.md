# 安全边界

安全边界规定Agent可以读取哪些数据、调用哪些工具、写入哪些目录以及代表谁执行操作。边界失效往往不是单一漏洞，而是身份、网络、文件系统和命令执行权限叠加后形成的越权路径，例如借由符号链接越过目录限制，或让不可信内容影响工具参数。设计时应采用最小权限、默认拒绝和明确的资源白名单，把只读与写入、测试环境与生产环境、个人数据与公开数据严格隔离。高风险操作需要二次校验和可撤销机制，外部输入始终视为不可信，敏感信息在日志中脱敏。若检测到越界尝试，应立即停止相关调用，保留审计记录，撤销泄露凭据，并重新评估整个执行链是否已被污染。
