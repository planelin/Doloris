# 工具死循环

工具死循环表现为Agent反复调用同一工具、参数几乎不变，却始终无法得到可接受的结论。原因可能是错误分类不当、成功条件模糊、返回结果被截断，或模型忽略了已经获得的证据。每次重复都会消耗时间、额度和上下文，因此必须设置调用次数、总时长和重复指纹上限。当检测到相同工具与参数再次出现时，应比较前后结果，要求调用方说明预期差异；若没有差异，就停止自动重试并转入诊断。修复方法包括缩小参数范围、补充前置条件、改进错误提示和增加结果校验。循环终止后还要记录触发原因，避免下一轮任务因相同模式再次失控。
