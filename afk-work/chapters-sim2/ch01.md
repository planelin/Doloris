# 流式中断

流式中断通常发生在模型已经返回部分内容之后，连接因网络抖动、服务重启或代理超时突然关闭。它与普通请求失败不同，调用方往往已经收到片段，却又无法确认最终状态。故障处理时，应为每个流分配唯一标识，记录最后成功接收的序号和内容摘要；恢复后优先询问服务端能否续传，不能续传时再基于检查点重启生成。界面需要明确标示未完成状态，禁止把半截答案当成正式结果，同时通过心跳、空闲超时和去重机制避免连接假活或重复输出。
