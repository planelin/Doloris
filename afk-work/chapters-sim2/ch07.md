# 进程僵死

进程僵死不同于正常退出或崩溃，它可能仍占用内存、文件描述符和端口，却不再处理输入、输出心跳或响应终止信号。常见原因是死锁、无限阻塞的系统调用、线程池枯竭、子进程未回收，以及标准输入输出管道被写满。监管程序不能只检查进程是否存在，还要观察CPU、内存、I/O、心跳时间和最近活跃任务，综合判断其是否失去进展。检测到僵死后，应先保存堆栈、日志和待处理队列，再尝试温和终止，给予清理钩子执行机会；若仍无响应，则强制结束并孤立运行目录。重启时限制并发和资源，确保旧进程的上游消费者已经切换，避免僵尸任务重复领取相同工作。
