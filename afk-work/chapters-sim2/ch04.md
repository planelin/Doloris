# 上下文污染

上下文污染是指错误、过期或彼此矛盾的信息进入推理窗口，并持续影响后续决策。常见来源包括失败的检索结果、被注入的网页文本、用户误述、工具返回值截断以及旧任务残留摘要。污染初期往往不易察觉，Agent可能引用不存在的文件、坚持错误前提或不断扩大无关上下文。治理时应为信息标注来源、时间和可信度，把事实、假设与指令分区保存，并在关键步骤前执行一致性检查。发现冲突后暂停写入，回溯最早出现分歧的位置，通过重新检索、隔离不可信内容和重建精简上下文恢复。必要时开启新会话，只迁移经过验证的状态，防止错误沿调用链传播。
